# ecom-user (Next.js frontend)

This repository contains the Next.js (TypeScript) frontend scaffold for the ecom-user project. It's organized feature-first and uses Tailwind CSS, React Query, Zod, react-hook-form and an API-proxy for secure HttpOnly cookie authentication.

See commits for feature branches. Each completed feature is pushed to its own branch and merged into main when approved.
